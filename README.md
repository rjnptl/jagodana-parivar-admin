# Jagodana Admin

Standalone admin UI for Jagodana Family Connect.

## Run Locally

1. Install dependencies:
   `npm install`
2. Start the admin app:
   `npm run dev`

Default admin credentials:

- Username: `admin`
- Password: `admin123`
